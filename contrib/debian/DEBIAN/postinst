#!/bin/sh

set -e

case "$1" in
    configure)
        adduser --system --disabled-password --disabled-login --home /var/lib/jerrybuild \
                --no-create-home --quiet --force-badname --group jerrybuild
        chown jerrybuild:jerrybuild /var/lib/jerrybuild -R
        ;;
esac

exit 0
